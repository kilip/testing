---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tasmoadmin-pvc
  namespace: home
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 128Mi
  storageClassName: longhorn
